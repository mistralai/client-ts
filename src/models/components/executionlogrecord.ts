/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 44424769d595
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ExecutionLogRecord = {
  timestamp: Date;
  traceId: string;
  spanId: string;
  severityText: string;
  body: string;
  logAttributes: { [k: string]: string };
};

/** @internal */
export const ExecutionLogRecord$inboundSchema: z.ZodType<
  ExecutionLogRecord,
  unknown
> = z.object({
  timestamp: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  trace_id: z.string(),
  span_id: z.string(),
  severity_text: z.string(),
  body: z.string(),
  log_attributes: z.record(z.string(), z.string()),
}).transform((v) => {
  return remap$(v, {
    "trace_id": "traceId",
    "span_id": "spanId",
    "severity_text": "severityText",
    "log_attributes": "logAttributes",
  });
});

export function executionLogRecordFromJSON(
  jsonString: string,
): SafeParseResult<ExecutionLogRecord, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ExecutionLogRecord$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ExecutionLogRecord' from JSON`,
  );
}
