/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: b592ff488956
 */

import * as z from "zod/v4";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";

/**
 * Controls what happens when a workflow would be started by a schedule but
 *
 * @remarks
 * one is already running.
 */
export const ScheduleOverlapPolicy = {
  One: 1,
  Two: 2,
  Three: 3,
  Four: 4,
  Five: 5,
  Six: 6,
} as const;
/**
 * Controls what happens when a workflow would be started by a schedule but
 *
 * @remarks
 * one is already running.
 */
export type ScheduleOverlapPolicy = OpenEnum<typeof ScheduleOverlapPolicy>;

/** @internal */
export const ScheduleOverlapPolicy$inboundSchema: z.ZodType<
  ScheduleOverlapPolicy,
  unknown
> = openEnums.inboundSchemaInt(ScheduleOverlapPolicy);
/** @internal */
export const ScheduleOverlapPolicy$outboundSchema: z.ZodType<
  number,
  ScheduleOverlapPolicy
> = openEnums.outboundSchemaInt(ScheduleOverlapPolicy);
