# RegistrySharingScope

## Example Usage

```typescript
import { RegistrySharingScope } from "@mistralai/mistralai/models/components";

let value: RegistrySharingScope = "sharing_scope_unspecified";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"sharing_scope_unspecified" | "private" | "workspace" | Unrecognized<string>
```