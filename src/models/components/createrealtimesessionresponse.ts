/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 54b042ff1e68
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { ClientSecret, ClientSecret$inboundSchema } from "./clientsecret.js";
import {
  ClientSessionPurpose,
  ClientSessionPurpose$inboundSchema,
} from "./clientsessionpurpose.js";

export type CreateRealtimeSessionResponse = {
  object: "client.session";
  /**
   * Supported purposes for client sessions.
   */
  purpose: ClientSessionPurpose;
  expiresAt: Date;
  clientSecret: ClientSecret;
};

/** @internal */
export const CreateRealtimeSessionResponse$inboundSchema: z.ZodType<
  CreateRealtimeSessionResponse,
  unknown
> = z.object({
  object: z.literal("client.session").default("client.session"),
  purpose: ClientSessionPurpose$inboundSchema,
  expires_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  client_secret: ClientSecret$inboundSchema,
}).transform((v) => {
  return remap$(v, {
    "expires_at": "expiresAt",
    "client_secret": "clientSecret",
  });
});

export function createRealtimeSessionResponseFromJSON(
  jsonString: string,
): SafeParseResult<CreateRealtimeSessionResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateRealtimeSessionResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateRealtimeSessionResponse' from JSON`,
  );
}
