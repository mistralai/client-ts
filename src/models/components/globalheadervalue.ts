/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 710f48b40205
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Value of a connector-wide header. ``value`` is plaintext in memory so create
 *
 * @remarks
 * round-trips and encryption-at-rest keep the real value; secrets are redacted only
 * on JSON serialization (API responses).
 */
export type GlobalHeaderValue = {
  isSecret?: boolean | undefined;
  value: string;
};

/** @internal */
export const GlobalHeaderValue$inboundSchema: z.ZodType<
  GlobalHeaderValue,
  unknown
> = z.object({
  is_secret: z.boolean().default(true),
  value: z.string(),
}).transform((v) => {
  return remap$(v, {
    "is_secret": "isSecret",
  });
});
/** @internal */
export type GlobalHeaderValue$Outbound = {
  is_secret: boolean;
  value: string;
};

/** @internal */
export const GlobalHeaderValue$outboundSchema: z.ZodType<
  GlobalHeaderValue$Outbound,
  GlobalHeaderValue
> = z.object({
  isSecret: z.boolean().default(true),
  value: z.string(),
}).transform((v) => {
  return remap$(v, {
    isSecret: "is_secret",
  });
});

export function globalHeaderValueToJSON(
  globalHeaderValue: GlobalHeaderValue,
): string {
  return JSON.stringify(
    GlobalHeaderValue$outboundSchema.parse(globalHeaderValue),
  );
}
export function globalHeaderValueFromJSON(
  jsonString: string,
): SafeParseResult<GlobalHeaderValue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GlobalHeaderValue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GlobalHeaderValue' from JSON`,
  );
}
