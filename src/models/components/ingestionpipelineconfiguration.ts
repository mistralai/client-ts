/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 74d3bc7355ec
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type IngestionPipelineConfiguration = {
  id: string;
  name: string;
  createdAt: Date;
  modifiedAt: Date;
};

/** @internal */
export const IngestionPipelineConfiguration$inboundSchema: z.ZodType<
  IngestionPipelineConfiguration,
  unknown
> = z.object({
  id: z.string(),
  name: z.string(),
  created_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  modified_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
}).transform((v) => {
  return remap$(v, {
    "created_at": "createdAt",
    "modified_at": "modifiedAt",
  });
});

export function ingestionPipelineConfigurationFromJSON(
  jsonString: string,
): SafeParseResult<IngestionPipelineConfiguration, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => IngestionPipelineConfiguration$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'IngestionPipelineConfiguration' from JSON`,
  );
}
