/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { fimComplete } from "../../funcs/fimComplete.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.FIMCompletionRequest$inboundSchema,
};

export const tool$fimComplete: ToolDefinition<typeof args> = {
  name: "fim-complete",
  description: `Fim Completion

FIM completion.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await fimComplete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
