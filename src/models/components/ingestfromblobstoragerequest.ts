/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f6e54b9b1f8a
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";

/**
 * Request body for ingesting a document from blob storage.
 */
export type IngestFromBlobStorageRequest = {
  /**
   * Name of the file to be ingested.
   */
  filename: string;
  /**
   * Signed URL for ingesting a document from blob storage. You can get one via the /libraries/{library_id}/documents/signed-upload-url endpoint.
   */
  signedUrl: string;
};

/** @internal */
export type IngestFromBlobStorageRequest$Outbound = {
  filename: string;
  signed_url: string;
};

/** @internal */
export const IngestFromBlobStorageRequest$outboundSchema: z.ZodType<
  IngestFromBlobStorageRequest$Outbound,
  IngestFromBlobStorageRequest
> = z.object({
  filename: z.string(),
  signedUrl: z.string(),
}).transform((v) => {
  return remap$(v, {
    signedUrl: "signed_url",
  });
});

export function ingestFromBlobStorageRequestToJSON(
  ingestFromBlobStorageRequest: IngestFromBlobStorageRequest,
): string {
  return JSON.stringify(
    IngestFromBlobStorageRequest$outboundSchema.parse(
      ingestFromBlobStorageRequest,
    ),
  );
}
