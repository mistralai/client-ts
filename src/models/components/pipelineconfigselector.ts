/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 0f9f0726a27d
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  SourceKind,
  SourceKind$inboundSchema,
  SourceKind$outboundSchema,
} from "./sourcekind.js";

export type PipelineConfigSelector = {
  sourceKind: SourceKind;
  filter?: string | null | undefined;
};

/** @internal */
export const PipelineConfigSelector$inboundSchema: z.ZodType<
  PipelineConfigSelector,
  unknown
> = z.object({
  source_kind: SourceKind$inboundSchema,
  filter: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    "source_kind": "sourceKind",
  });
});
/** @internal */
export type PipelineConfigSelector$Outbound = {
  source_kind: string;
  filter?: string | null | undefined;
};

/** @internal */
export const PipelineConfigSelector$outboundSchema: z.ZodType<
  PipelineConfigSelector$Outbound,
  PipelineConfigSelector
> = z.object({
  sourceKind: SourceKind$outboundSchema,
  filter: z.nullable(z.string()).optional(),
}).transform((v) => {
  return remap$(v, {
    sourceKind: "source_kind",
  });
});

export function pipelineConfigSelectorToJSON(
  pipelineConfigSelector: PipelineConfigSelector,
): string {
  return JSON.stringify(
    PipelineConfigSelector$outboundSchema.parse(pipelineConfigSelector),
  );
}
export function pipelineConfigSelectorFromJSON(
  jsonString: string,
): SafeParseResult<PipelineConfigSelector, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PipelineConfigSelector$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PipelineConfigSelector' from JSON`,
  );
}
