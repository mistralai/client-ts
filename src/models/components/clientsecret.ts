/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: f15778761426
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type ClientSecret = {
  value: string;
  expiresAt: Date;
};

/** @internal */
export const ClientSecret$inboundSchema: z.ZodType<ClientSecret, unknown> = z
  .object({
    value: z.string(),
    expires_at: z.iso.datetime({ offset: true }).transform(v => new Date(v)),
  }).transform((v) => {
    return remap$(v, {
      "expires_at": "expiresAt",
    });
  });

export function clientSecretFromJSON(
  jsonString: string,
): SafeParseResult<ClientSecret, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ClientSecret$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ClientSecret' from JSON`,
  );
}
