# GetSearchIndexSummaryResponseIndexStatus

## Example Usage

```typescript
import { GetSearchIndexSummaryResponseIndexStatus } from "@mistralai/mistralai/models/components";

let value: GetSearchIndexSummaryResponseIndexStatus = "offline";
```

## Values

This is an open enum. Unrecognized values will be captured as the `Unrecognized<string>` branded type.

```typescript
"online" | "offline" | Unrecognized<string>
```