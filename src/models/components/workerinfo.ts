/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: 37a3804dd49f
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type WorkerInfo = {
  schedulerUrl: string;
  namespace: string;
  tls: boolean;
};

/** @internal */
export const WorkerInfo$inboundSchema: z.ZodType<WorkerInfo, unknown> = z
  .object({
    scheduler_url: z.string(),
    namespace: z.string(),
    tls: z.boolean().default(false),
  }).transform((v) => {
    return remap$(v, {
      "scheduler_url": "schedulerUrl",
    });
  });

export function workerInfoFromJSON(
  jsonString: string,
): SafeParseResult<WorkerInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => WorkerInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'WorkerInfo' from JSON`,
  );
}
